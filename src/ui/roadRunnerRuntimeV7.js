const SAVE_KEY = '365_canvas_road_runner_v5';

const ROUTES = {
  track: { label: '365 Test Track', profile: 'track', length: 9800, meters: 3200, reward: 1.05, difficulty: 0.70, skyA: '#7ddcff', skyB: '#d9fbff', grass: '#58b957', road: '#2d3748', seed: 4, unlock: 0 },
  barangay: { label: 'Barangay Route', profile: 'barangay', length: 10800, meters: 3500, reward: 1.0, difficulty: 0.95, skyA: '#8bdcff', skyB: '#e1fbff', grass: '#5fbf57', road: '#3a4555', seed: 1, unlock: 600 },
  farm: { label: 'Farm Supply Run', profile: 'farm', length: 11800, meters: 3900, reward: 1.15, difficulty: 1.08, skyA: '#9ee8ff', skyB: '#e7fcff', grass: '#66bd45', road: '#554537', seed: 3, unlock: 1200 },
  mountain: { label: 'Mountain Parts Run', profile: 'mountain', length: 13800, meters: 4600, reward: 1.25, difficulty: 1.35, skyA: '#86d4ff', skyB: '#e0f7ff', grass: '#4f9e52', road: '#39414d', seed: 2, unlock: 1900 },
  port: { label: 'Port Export Route', profile: 'port', length: 15000, meters: 5000, reward: 1.35, difficulty: 1.45, skyA: '#93dfff', skyB: '#e4fbff', grass: '#49a985', road: '#36414d', seed: 5, unlock: 2700 }
};

const GHOST_MODES = { solo: { label: 'Solo', count: 0 }, ghost2: { label: '1 Ghost', count: 1 }, ghost3: { label: '2 Ghosts', count: 2 }, ghost4: { label: '3 Ghosts', count: 3 } };
const VEHICLES = {
  hatchback: { label: 'Starter Hatchback', asset: 'hatchback', unlock: { coins: 0, parts: 0 }, speed: 1, accel: 1, fuel: 1, handling: 1, durability: 1, description: 'Balanced starter car.' },
  greenCompact: { label: 'Compact Sport', asset: 'greenCompact', unlock: { coins: 180, parts: 1 }, speed: 1.08, accel: 1.08, fuel: 0.96, handling: 1.05, durability: 0.95, description: 'Faster compact for early ghost runs.' },
  pickup: { label: 'Parts Pickup', asset: 'pickup', unlock: { coins: 360, parts: 3 }, speed: 0.96, accel: 0.95, fuel: 1.2, handling: 1.08, durability: 1.22, description: 'More fuel and durability.' },
  serviceVan: { label: 'Service Van', asset: 'serviceVan', unlock: { coins: 720, parts: 6 }, speed: 0.92, accel: 0.90, fuel: 1.35, handling: 1.04, durability: 1.35, description: 'Endurance route vehicle.' },
  offroad: { label: 'Off-Road Truck', asset: 'offroad', unlock: { coins: 1100, parts: 10 }, speed: 1, accel: 1, fuel: 1.18, handling: 1.32, durability: 1.45, description: 'Best for rough roads.' },
  race: { label: 'Purple Race Coupe', asset: 'race', unlock: { coins: 1450, parts: 12 }, speed: 1.24, accel: 1.18, fuel: 0.90, handling: 1.08, durability: 0.85, description: 'Fastest track vehicle.' }
};
const UPGRADES = {
  engine: { label: 'Engine', description: 'More acceleration.', max: 15, baseCoins: 45, baseParts: 0, growth: 1.42, partsEvery: 4 },
  tires: { label: 'Tires', description: 'Higher speed, grip, and less drag.', max: 15, baseCoins: 40, baseParts: 0, growth: 1.40, partsEvery: 3 },
  fuelTank: { label: 'Fuel Tank', description: 'More fuel range and station capacity.', max: 15, baseCoins: 55, baseParts: 0, growth: 1.45, partsEvery: 3 },
  suspension: { label: 'Suspension', description: 'Better body control and less hazard damage.', max: 15, baseCoins: 45, baseParts: 0, growth: 1.42, partsEvery: 3 },
  durability: { label: 'Durability', description: 'Reduces wear and tear damage.', max: 15, baseCoins: 60, baseParts: 1, growth: 1.50, partsEvery: 2 },
  transmission: { label: 'Transmission', description: 'Better hill climbing and reverse recovery.', max: 10, baseCoins: 95, baseParts: 1, growth: 1.55, partsEvery: 2 },
  brakes: { label: 'Brakes', description: 'Slows quicker and reduces hazard damage while braking.', max: 10, baseCoins: 70, baseParts: 0, growth: 1.46, partsEvery: 3 },
  repairKit: { label: 'Repair Kit', description: 'Tool pickups repair more wear.', max: 10, baseCoins: 85, baseParts: 1, growth: 1.48, partsEvery: 2 }
};
const MISSIONS = [
  { key: 'fuel-run', label: 'Reach Next Fuel', description: 'Reach 900m with at least 10% fuel.', target: 900, minFuelPct: 10, rewardCoins: 70, rewardParts: 0 },
  { key: 'parts-salvage', label: 'Salvage Parts', description: 'Collect 4 parts in one run.', targetParts: 4, rewardCoins: 45, rewardParts: 2 },
  { key: 'smooth-driver', label: 'Smooth Driver', description: 'Reach 1200m with less than 45% wear.', target: 1200, maxWearPct: 45, rewardCoins: 90, rewardParts: 1 },
  { key: 'repair-run', label: 'Use Repairs', description: 'Use a mechanic shop or tool pickup during a run.', needRepair: true, rewardCoins: 75, rewardParts: 1 },
  { key: 'hazard-control', label: 'Hazard Control', description: 'Pass 3 hazards while keeping wear under 60%.', targetHazards: 3, maxWearPct: 60, rewardCoins: 120, rewardParts: 1 }
];
const ASSET_PATHS = {
  hatchback: '/assets/vehicles/car-compact-blue.svg', greenCompact: '/assets/vehicles/car-compact-green.svg', pickup: '/assets/vehicles/pickup-orange.svg', serviceVan: '/assets/vehicles/van-service-white.svg', offroad: '/assets/vehicles/offroad-red.svg', race: '/assets/vehicles/race-purple.svg',
  ghostA: '/assets/vehicles/car-compact-green.svg', ghostB: '/assets/vehicles/pickup-orange.svg', ghostC: '/assets/vehicles/van-service-white.svg', coin: '/assets/road-runner/token-coin.svg', fuel: '/assets/road-runner/token-energy.svg', parts: '/assets/road-runner/token-parts.svg', tools: '/assets/road-runner/tool-kit.svg', fuelStation: '/assets/road-runner/fuel-station.svg', partsStore: '/assets/road-runner/parts-store.svg', mechanicShop: '/assets/road-runner/mechanic-shop.svg', wreck: '/assets/road-runner/wrecked-car.svg'
};
const DAMAGE_BANDS = [
  { label: 'ZERO DMG', max: 8, damage: 0, event: 'Zero damage' },
  { label: 'SMALL', max: 16, damage: 1.2, event: 'Small bump' },
  { label: 'MINOR', max: 26, damage: 3.4, event: 'Minor damage' },
  { label: 'MODERATE', max: 38, damage: 7.2, event: 'Moderate damage' },
  { label: 'LOTS', max: 54, damage: 13.5, event: 'Lots of damage' },
  { label: 'EPIC DMG', max: Infinity, damage: 24, event: 'Epic damage' }
];
let canvas = null, ctx = null, frameId = 0, mountedScreen = null, activeRoute = 'track', activeGhostMode = 'ghost2', saveData = loadSave(), images = loadImages(), input = { gas: false, brake: false }, game = null, lastTime = 0;
function clamp(v, min, max) { return Math.max(min, Math.min(max, v)); }
function defaultUpgrades() { return { engine: 1, tires: 1, fuelTank: 1, suspension: 1, durability: 1, transmission: 1, brakes: 1, repairKit: 1 }; }
function normalizeUpgrades(upgrades = {}) { const migrated = { ...upgrades, fuelTank: upgrades.fuelTank || upgrades.energyTank || 1, transmission: upgrades.transmission || 1, brakes: upgrades.brakes || 1, repairKit: upgrades.repairKit || 1 }; const result = {}; Object.keys(defaultUpgrades()).forEach((key) => { result[key] = clamp(Number(migrated[key] || 1), 1, UPGRADES[key].max); }); return result; }
function loadSave() { try { const raw = localStorage.getItem(SAVE_KEY) || localStorage.getItem('365_canvas_road_runner_v4') || localStorage.getItem('365_canvas_road_runner_v3') || localStorage.getItem('365_canvas_road_runner_v2') || localStorage.getItem('365_canvas_road_runner_v1') || '{}'; const parsed = JSON.parse(raw); const unlockedVehicles = Array.isArray(parsed.unlockedVehicles) && parsed.unlockedVehicles.length ? parsed.unlockedVehicles : ['hatchback']; return { coins: parsed.coins || 0, parts: parsed.parts || 0, tools: parsed.tools || 0, bestDistance: parsed.bestDistance || 0, bestTrail: Array.isArray(parsed.bestTrail) ? parsed.bestTrail : [], upgrades: normalizeUpgrades(parsed.upgrades), unlockedVehicles, selectedVehicle: unlockedVehicles.includes(parsed.selectedVehicle) ? parsed.selectedVehicle : 'hatchback', completedMissions: Array.isArray(parsed.completedMissions) ? parsed.completedMissions : [], lifetimeFuel: parsed.lifetimeFuel || 0, lifetimeWear: parsed.lifetimeWear || 0, lifetimeRepairs: parsed.lifetimeRepairs || 0 }; } catch { return { coins: 0, parts: 0, tools: 0, bestDistance: 0, bestTrail: [], upgrades: defaultUpgrades(), unlockedVehicles: ['hatchback'], selectedVehicle: 'hatchback', completedMissions: [], lifetimeFuel: 0, lifetimeWear: 0, lifetimeRepairs: 0 }; } }
function saveGameData() { localStorage.setItem(SAVE_KEY, JSON.stringify(saveData)); }
function formatSmall(value) { if (value >= 1000000) return `${(value / 1000000).toFixed(1)}M`; if (value >= 1000) return `${(value / 1000).toFixed(1)}K`; return `${Math.floor(value)}`; }
function upgradeCost(key, level) { const item = UPGRADES[key]; if (!item || level >= item.max) return { coins: Infinity, parts: Infinity }; return { coins: Math.round(item.baseCoins * Math.pow(item.growth, level - 1)), parts: item.baseParts + Math.floor(level / item.partsEvery) }; }
function selectedVehicle() { return VEHICLES[saveData.selectedVehicle] || VEHICLES.hatchback; }
function routeUnlocked(key) { return (saveData.bestDistance || 0) >= (ROUTES[key].unlock || 0); }
function vehicleStats() { const u = normalizeUpgrades(saveData.upgrades); const v = selectedVehicle(); return { acceleration: (82 + u.engine * 8) * v.accel, topSpeed: (132 + u.tires * 6) * v.speed, reverseTop: 24 + u.transmission * 3, drag: Math.max(3, 11 - u.tires * 0.35), grip: 0.78 + u.tires * 0.022, maxFuel: (95 + u.fuelTank * 20) * v.fuel, fuelUse: Math.max(1.6, 4.0 - u.fuelTank * 0.08), spring: 4.8 + u.suspension * 0.42, damping: 2.7 + u.suspension * 0.18, durability: (1 + u.durability * 0.1) * v.durability, wearLimit: 100 + u.durability * 12, climb: 1 + u.transmission * 0.045, brakePower: 170 + u.brakes * 11, repairPower: 6 + u.repairKit * 2 }; }
function routeMeters(route, pixels) { return Math.floor(Math.max(0, pixels) * (route.meters / route.length)); }
function speedKmh() { return Math.abs(game?.speed || 0) * 0.52; }
function damageBand(kmh = speedKmh()) { return DAMAGE_BANDS.find((band) => kmh <= band.max) || DAMAGE_BANDS[DAMAGE_BANDS.length - 1]; }
function routeY(route, x, h) { const base = Math.max(170, Math.min(h - 130, h * 0.61)), s = route.seed || 1; if (route.profile === 'track') return base + Math.sin((x + s * 90) / 430) * 14 + Math.sin((x + s * 40) / 190) * 6 + Math.sin((x + s * 20) / 820) * 16; if (route.profile === 'mountain') return base + Math.sin((x + s * 160) / 180) * 42 + Math.sin((x + s * 85) / 74) * 13 - Math.sin((x + s * 60) / 520) * 46; if (route.profile === 'farm') return base + Math.sin((x + s * 120) / 240) * 26 + Math.sin((x + s * 70) / 96) * 10 - Math.sin((x + s * 30) / 680) * 22; if (route.profile === 'port') return base + Math.sin((x + s * 110) / 360) * 20 + Math.sin((x + s * 55) / 155) * 7 - Math.sin((x + s * 40) / 940) * 24; return base + Math.sin((x + s * 120) / 175) * 24 + Math.sin((x + s * 70) / 78) * 9 - Math.sin((x + s * 30) / 540) * 26; }
function routeAngle(route, x, h) { const rise = routeY(route, x + 10, h) - routeY(route, x - 10, h); return clamp((rise / 20) * 0.42, -0.55, 0.55); }
function loadImages() { const r = {}; Object.entries(ASSET_PATHS).forEach(([k, p]) => { const img = new Image(); img.onload = () => { img.ready = true; }; img.onerror = () => { img.ready = false; }; img.src = p; r[k] = img; }); return r; }
function seededNoise(x, seed) { return Math.abs(Math.sin(x * 0.013 + seed * 9.17) * 43758.5453) % 1; }
function makePickups(route) { const a = [], spacing = route.profile === 'track' ? 260 : 230; for (let x = 360; x < route.length - 260; x += spacing) { const roll = seededNoise(x, route.seed); const type = roll > 0.90 ? 'tools' : roll > 0.72 ? 'parts' : roll > 0.55 ? 'fuel' : 'coin'; a.push({ x, type, collected: false }); } return a; }
function makeDirector(route) { const checkpoints = [], hazards = []; let fuelX = route.profile === 'track' ? 850 : 1100; const fuelGap = route.profile === 'track' ? 1550 : route.profile === 'mountain' ? 2450 : route.profile === 'port' ? 2650 : 1900; while (fuelX < route.length - 620) { fuelX += fuelGap + (seededNoise(fuelX, route.seed) - 0.5) * 760 * route.difficulty; if (fuelX < route.length - 520) checkpoints.push({ x: fuelX, type: 'fuelStation', used: false }); } for (let x = 1350; x < route.length - 700; x += 2600 + seededNoise(x, route.seed) * 500) checkpoints.push({ x, type: 'partsStore', used: false }); for (let x = 1950; x < route.length - 800; x += 3100 + seededNoise(x, route.seed) * 650) checkpoints.push({ x, type: 'mechanicShop', used: false }); for (let x = 850; x < route.length - 600; x += 1500 + seededNoise(x, route.seed) * 600) checkpoints.push({ x, type: 'wreck', used: false }); const types = ['pothole', 'mud', 'gravel', 'rough']; for (let x = 720; x < route.length - 500; x += 620 + seededNoise(x, route.seed) * 520) { const type = types[Math.floor(seededNoise(x + 33, route.seed) * types.length)]; const length = type === 'pothole' ? 90 : type === 'rough' ? 260 : type === 'mud' ? 210 : 230; hazards.push({ x, type, length, impacted: false, entered: false }); } return { checkpoints: checkpoints.sort((a, b) => a.x - b.x), hazards: hazards.sort((a, b) => a.x - b.x) }; }
function currentMission() { return MISSIONS.find((m) => !saveData.completedMissions.includes(m.key)) || MISSIONS[MISSIONS.length - 1]; }
function resetRun() { const route = ROUTES[activeRoute], stats = vehicleStats(), director = makeDirector(route); game = { route, stats, mission: currentMission(), x: 80, speed: 0, fuel: stats.maxFuel, distancePx: 0, distanceM: 0, coins: 0, parts: 0, tools: 0, wear: 0, pitch: 0, pitchVel: 0, suspensionTravel: 0, elapsed: 0, finished: false, pickups: makePickups(route), checkpoints: director.checkpoints, hazards: director.hazards, samples: [], lastSample: 0, message: '', eventText: '', eventTimer: 0, hazardsCleared: 0, usedRepair: false, ghostColors: ['#22c55e', '#f97316', '#e5e7eb'] }; hideEndPanel(); updateHud(); updateTelemetry(); }
function shellHtml() { return `<section class="card roadRunnerShell"><div class="roadRunnerHeader"><h2>365 Hill Route - Physics V7</h2><p>Runtime speedometer, speed-class hazard damage, repair shops, tools, reverse, and missions.</p></div><div class="roadRunnerHud"><div class="roadRunnerStat"><b data-rr-distance>0m</b><span>Distance</span></div><div class="roadRunnerStat"><b data-rr-fuel>100%</b><span>Fuel</span></div><div class="roadRunnerStat"><b data-rr-wear>0%</b><span>Wear</span></div><div class="roadRunnerStat"><b data-rr-coins>${formatSmall(saveData.coins)}</b><span>Coins</span></div><div class="roadRunnerStat"><b data-rr-parts>${formatSmall(saveData.parts)}</b><span>Parts</span></div><div class="roadRunnerStat"><b data-rr-best>${formatSmall(saveData.bestDistance)}m</b><span>Best</span></div></div><div class="roadRunnerQuickPanel"><button class="btn primary" onclick="window.restartHillRoute?.()">Restart Run</button><div class="roadRunnerVs" data-rr-vs></div><div class="roadRunnerLegend"><span><i class="coin"></i>Coins</span><span><i class="energy"></i>Fuel</span><span><i class="parts"></i>Parts</span><div class="racerRepairHint">Speed classes: ZERO, SMALL, MINOR, MODERATE, LOTS, EPIC. Slow before potholes.</div></div></div><div class="roadRunnerGameFrame"><div id="roadRunnerGameHost"><canvas id="roadRunnerCanvas"></canvas></div><div class="roadRunnerOverlay"><div class="roadRunnerBadge" data-rr-route>${ROUTES[activeRoute].label}</div><div class="roadRunnerBadge" data-rr-mode>${GHOST_MODES[activeGhostMode].label}</div></div><div class="roadRunnerControlHint">BRAKE slows. Hold BRAKE when stopped to reverse. Pothole impact damage depends on KM/H.</div><div class="roadRunnerControls"><button class="roadRunnerPedal brake" data-rr-control="brake">BRAKE / REV</button><button class="roadRunnerPedal gas" data-rr-control="gas">GAS</button></div><div class="roadRunnerEndPanel" hidden data-rr-end-panel><h3 data-rr-end-title>Run Finished</h3><p data-rr-end-copy></p><button class="btn primary" onclick="window.restartHillRoute?.()">Restart Run</button></div></div><section class="roadRunnerPanel"><div class="roadRunnerPanelTitle"><h3>Ghost Race</h3><span class="roadRunnerNotice">Computer ghosts or saved best trail.</span></div><div class="roadRunnerModeGrid">${Object.entries(GHOST_MODES).map(([k, v]) => `<button class="btn ${k === activeGhostMode ? 'primary' : 'ghost'}" onclick="window.setHillGhosts?.('${k}')">${v.label}</button>`).join('')}</div></section><section class="roadRunnerPanel"><div class="roadRunnerPanelTitle"><h3>Routes</h3><span class="roadRunnerNotice">Routes unlock by best distance.</span></div><div class="roadRunnerModeGrid">${Object.entries(ROUTES).map(([k, v]) => `<button class="btn ${k === activeRoute ? 'gold' : 'ghost'}" ${routeUnlocked(k) ? '' : 'disabled'} onclick="window.setHillRoute?.('${k}')">${routeUnlocked(k) ? v.label : `Locked ${v.unlock}m`}</button>`).join('')}</div></section><section class="roadRunnerPanel" data-road-runner-missions></section><section class="roadRunnerPanel" data-road-runner-vehicles></section><section class="roadRunnerPanel" data-road-runner-garage></section></section>`; }
function setText(s, v) { const e = document.querySelector(s); if (e) e.textContent = v; }
function updateHud() { if (!game) return; setText('[data-rr-distance]', `${Math.floor(game.distanceM)}m`); setText('[data-rr-fuel]', `${Math.max(0, Math.floor(game.fuel / game.stats.maxFuel * 100))}%`); setText('[data-rr-wear]', `${Math.floor(Math.min(100, game.wear / game.stats.wearLimit * 100))}%`); setText('[data-rr-coins]', formatSmall(saveData.coins)); setText('[data-rr-parts]', formatSmall(saveData.parts)); setText('[data-rr-best]', `${formatSmall(saveData.bestDistance)}m`); setText('[data-rr-route]', ROUTES[activeRoute].label); setText('[data-rr-mode]', GHOST_MODES[activeGhostMode].label); setText('[data-rr-vs]', `You: ${selectedVehicle().label} | Next fuel: ${nextFuelMeters()}m | Hazard: ${nextHazardMeters()}m`); renderMissionPanel(); renderVehiclePanel(); renderGaragePanel(); }
function nextFuelMeters() { if (!game) return 0; const n = game.checkpoints.find(c => !c.used && c.type === 'fuelStation' && c.x > game.x); return n ? Math.max(0, routeMeters(game.route, n.x - game.x)) : 0; }
function nextHazardMeters() { if (!game) return 0; const n = game.hazards.find(h => h.x + h.length > game.x); return n ? Math.max(0, routeMeters(game.route, n.x - game.x)) : 0; }
function renderMissionPanel() { const p = document.querySelector('[data-road-runner-missions]'); if (!p || !game) return; const m = game.mission; p.innerHTML = `<div class="roadRunnerPanelTitle"><h3>Missions</h3><span class="roadRunnerNotice">Complete contracts for bonus rewards.</span></div><div class="racerHubCards"><div class="racerHubCard"><h4>${m.label}</h4><p>${m.description}</p><p>Reward: ${m.rewardCoins} coins / ${m.rewardParts} parts</p><p>Status: ${missionComplete() ? 'Ready to complete' : 'In progress'}</p></div><div class="racerHubCard"><h4>Hazard Driving</h4><p>Potholes use speed-at-impact. Braking and low KM/H reduce or remove damage.</p></div></div>`; }
function renderVehiclePanel() { const p = document.querySelector('[data-road-runner-vehicles]'); if (!p) return; p.innerHTML = `<div class="roadRunnerPanelTitle"><h3>Vehicle Selection</h3><span class="roadRunnerNotice">Vehicles change speed, fuel, handling, and durability.</span></div><div class="roadRunnerVehicleGrid">${Object.entries(VEHICLES).map(([k, i]) => vehicleCard(k, i)).join('')}</div>`; }
function vehicleCard(k, i) { const unlocked = saveData.unlockedVehicles.includes(k), selected = saveData.selectedVehicle === k, can = saveData.coins >= i.unlock.coins && saveData.parts >= i.unlock.parts, action = unlocked ? `window.selectRoadRunnerVehicle?.('${k}')` : `window.unlockRoadRunnerVehicle?.('${k}')`, img = ASSET_PATHS[i.asset]; return `<div class="roadRunnerVehicleCard ${selected ? 'selected' : ''}"><div class="vehicleThumb">${img ? `<img src="${img}" alt="${i.label}"/>` : ''}</div><h4>${i.label}</h4><p>${i.description}</p><div class="vehicleStats">SPD ${i.speed.toFixed(2)} • FUEL ${i.fuel.toFixed(2)} • DUR ${i.durability.toFixed(2)}</div><button class="btn ${selected ? 'gold' : unlocked || can ? 'primary' : 'ghost'}" ${selected || (!unlocked && !can) ? 'disabled' : ''} onclick="${action}">${selected ? 'Selected' : unlocked ? 'Select' : can ? `Unlock ${formatSmall(i.unlock.coins)}C / ${i.unlock.parts}P` : `Locked ${formatSmall(i.unlock.coins)}C / ${i.unlock.parts}P`}</button></div>`; }
function renderGaragePanel() { const p = document.querySelector('[data-road-runner-garage]'); if (!p) return; p.innerHTML = `<div class="roadRunnerPanelTitle"><h3>Garage Upgrades</h3><span class="roadRunnerNotice">Upgrade speed, brakes, hazard control, repairs, and reverse.</span></div><div class="roadRunnerWalletRow"><div class="roadRunnerWalletPill">Coins: ${formatSmall(saveData.coins)}</div><div class="roadRunnerWalletPill">Parts: ${formatSmall(saveData.parts)}</div><div class="roadRunnerWalletPill">Tools: ${formatSmall(saveData.tools || 0)}</div></div><div class="roadRunnerGarageGrid">${Object.entries(UPGRADES).map(([k, i]) => upgradeCard(k, i)).join('')}</div>`; }
function upgradeCard(k, i) { const l = saveData.upgrades[k] || 1, max = l >= i.max, c = upgradeCost(k, l), can = !max && saveData.coins >= c.coins && saveData.parts >= c.parts; return `<div class="roadRunnerUpgradeCard"><h4>${i.label} Lv.${l}</h4><p>${i.description}</p><div class="roadRunnerUpgradeMeta"><span>${max ? 'Max' : `Next Lv.${l + 1}`}</span><span>${max ? 'MAX' : `${formatSmall(c.coins)} coins - ${c.parts} parts`}</span></div><button class="btn ${can ? 'primary' : 'ghost'}" ${can ? '' : 'disabled'} onclick="window.buyRoadRunnerUpgrade?.('${k}')">${max ? 'Maxed' : 'Upgrade'}</button></div>`; }
function showEndPanel(title, copy) { const p = document.querySelector('[data-rr-end-panel]'); if (!p) return; p.querySelector('[data-rr-end-title]').textContent = title; p.querySelector('[data-rr-end-copy]').textContent = copy; p.hidden = false; }
function hideEndPanel() { const p = document.querySelector('[data-rr-end-panel]'); if (p) p.hidden = true; }
function draw() { if (!canvas || !ctx || !game) return; const w = canvas.width, h = canvas.height, r = game.route, cam = clamp(game.x - w * 0.34, 0, Math.max(0, r.length - w + 260)); drawBackground(w, h, r, cam); drawRoad(w, h, r, cam); drawHazards(w, h, r, cam); drawCheckpoints(w, h, r, cam); drawPickups(w, h, r, cam); drawGhosts(w, h, r, cam); drawPlayer(w, h, r, cam); drawFinish(w, h, r, cam); drawRunMeters(w, h); }
function drawBackground(w, h, r, cam) { const sky = ctx.createLinearGradient(0, 0, 0, h); sky.addColorStop(0, r.skyA); sky.addColorStop(0.58, r.skyB); sky.addColorStop(0.59, r.grass); sky.addColorStop(1, '#2f8f3f'); ctx.fillStyle = sky; ctx.fillRect(0, 0, w, h); }
function drawRoad(w, h, r, cam) { const start = Math.max(0, cam - 80), end = Math.min(r.length + 260, cam + w + 140); ctx.fillStyle = r.grass; ctx.beginPath(); ctx.moveTo(0, h); for (let wx = start; wx <= end; wx += 18) ctx.lineTo(wx - cam, routeY(r, wx, h) + 38); ctx.lineTo(end - cam, h); ctx.closePath(); ctx.fill(); ctx.strokeStyle = r.road; ctx.lineWidth = r.profile === 'track' ? 34 : 28; ctx.lineCap = 'round'; ctx.lineJoin = 'round'; ctx.beginPath(); for (let wx = start; wx <= end; wx += 14) { const x = wx - cam, y = routeY(r, wx, h); if (wx === start) ctx.moveTo(x, y); else ctx.lineTo(x, y); } ctx.stroke(); ctx.strokeStyle = 'rgba(248,250,252,.62)'; ctx.lineWidth = 4; for (let wx = Math.floor(start / 120) * 120; wx <= end; wx += 120) { ctx.beginPath(); ctx.moveTo(wx - cam, routeY(r, wx, h) - 1); ctx.lineTo(wx + 58 - cam, routeY(r, wx + 58, h) - 1); ctx.stroke(); } }
function drawHazards(w, h, r, cam) { for (const z of game.hazards) { const x = z.x - cam; if (x < -120 || x > w + 120) continue; const y = routeY(r, z.x, h) + 4; ctx.fillStyle = z.type === 'mud' ? 'rgba(101,67,33,.75)' : z.type === 'gravel' ? 'rgba(203,213,225,.75)' : z.type === 'rough' ? 'rgba(100,116,139,.75)' : 'rgba(15,23,42,.82)'; ctx.beginPath(); ctx.ellipse(x + z.length / 2, y, z.length / 2, 10, 0, 0, Math.PI * 2); ctx.fill(); drawOutlinedText(z.type.toUpperCase(), x + z.length / 2, y - 18, '10px'); } }
function drawCheckpoints(w, h, r, cam) { for (const it of game.checkpoints) { const x = it.x - cam; if (x < -120 || x > w + 120) continue; const y = routeY(r, it.x, h) - 76, img = images[it.type]; if (img && img.ready) ctx.drawImage(img, x - 42, y - 34, 84, 68); else { ctx.fillStyle = it.type === 'fuelStation' ? '#22c55e' : it.type === 'partsStore' ? '#f97316' : it.type === 'mechanicShop' ? '#2563eb' : '#94a3b8'; ctx.fillRect(x - 34, y - 24, 68, 48); } const label = it.type === 'fuelStation' ? 'FUEL STOP' : it.type === 'partsStore' ? 'PARTS STORE' : it.type === 'mechanicShop' ? 'MECHANIC' : 'SALVAGE'; drawOutlinedText(it.used ? 'USED' : label, x, y - 40, '10px'); } }
function drawPickups(w, h, r, cam) { for (const p of game.pickups) { if (p.collected) continue; const x = p.x - cam; if (x < -60 || x > w + 60) continue; const y = routeY(r, p.x, h) - 34, img = images[p.type]; if (img && img.ready) ctx.drawImage(img, x - 20, y - 20, 40, 40); else { ctx.fillStyle = p.type === 'fuel' ? '#38bdf8' : p.type === 'parts' ? '#a78bfa' : p.type === 'tools' ? '#f97316' : '#facc15'; ctx.beginPath(); ctx.arc(x, y, 17, 0, Math.PI * 2); ctx.fill(); } } }
function drawGhosts(w, h, r, cam) { const mode = GHOST_MODES[activeGhostMode], keys = ['ghostA', 'ghostB', 'ghostC']; for (let i = 0; i < mode.count; i++) { const p = ghostPoint(i, h), x = p.x - cam; if (x < -100 || x > w + 120) continue; drawCar(x, p.y, routeAngle(r, p.x, h) * 0.65, images[keys[i]], i === 0 && saveData.bestTrail.length ? 'Best Ghost' : `Computer ${i + 1}`, 0.5, game.ghostColors[i]); } }
function ghostPoint(i, h) { const r = game.route; if (i === 0 && saveData.bestTrail.length > 5) { const target = Math.max(0, game.elapsed - i * 0.8); let sample = saveData.bestTrail[saveData.bestTrail.length - 1]; for (const item of saveData.bestTrail) { if (item.t >= target) { sample = item; break; } } return { x: sample.x, y: routeY(r, sample.x, h) - 30 }; } const pace = 0.9 + i * 0.08, x = 80 + Math.max(0, game.elapsed - i * 0.75) * (game.stats.topSpeed * pace * 0.58); return { x, y: routeY(r, x, h) - 30 }; }
function drawPlayer(w, h, r, cam) { const x = game.x - cam, y = routeY(r, game.x, h) - 30; drawCar(x, y, game.pitch, images[selectedVehicle().asset], 'You', 1, '#3b82f6'); }
function drawCar(x, y, angle, img, label, alpha, color) { ctx.save(); ctx.translate(x, y); ctx.rotate(angle); ctx.globalAlpha = alpha; ctx.fillStyle = 'rgba(0,0,0,.22)'; ctx.beginPath(); ctx.ellipse(0, 22, 48, 10, 0, 0, Math.PI * 2); ctx.fill(); if (img && img.ready) ctx.drawImage(img, -56, -31, 112, 56); else { ctx.fillStyle = color; roundRect(-36, -15, 72, 30, 9); ctx.fill(); } ctx.strokeStyle = '#111827'; ctx.lineWidth = 2; ctx.beginPath(); ctx.moveTo(-28, 18 + game.suspensionTravel); ctx.lineTo(-28, 30); ctx.moveTo(28, 18 - game.suspensionTravel); ctx.lineTo(28, 30); ctx.stroke(); ctx.restore(); drawOutlinedText(label, x, y - 46, '11px', alpha); }
function roundRect(x, y, w, h, r) { ctx.beginPath(); ctx.moveTo(x + r, y); ctx.lineTo(x + w - r, y); ctx.quadraticCurveTo(x + w, y, x + w, y + r); ctx.lineTo(x + w, y + h - r); ctx.quadraticCurveTo(x + w, y + h, x + w - r, y + h); ctx.lineTo(x + r, y + h); ctx.quadraticCurveTo(x, y + h, x, y + h - r); ctx.lineTo(x, y + r); ctx.quadraticCurveTo(x, y, x + r, y); }
function drawOutlinedText(text, x, y, size = '11px', alpha = 1) { ctx.globalAlpha = alpha; ctx.fillStyle = '#fff'; ctx.font = `900 ${size} Arial`; ctx.textAlign = 'center'; ctx.strokeStyle = '#000'; ctx.lineWidth = 3; ctx.strokeText(text, x, y); ctx.fillText(text, x, y); ctx.globalAlpha = 1; }
function drawFinish(w, h, r, cam) { const x = r.length - cam; if (x < -20 || x > w + 30) return; const y = routeY(r, r.length, h); ctx.fillStyle = '#fff'; ctx.fillRect(x - 5, y - 70, 10, 90); }
function drawRunMeters(w, h) { drawBar(14, 42, 150, 11, 'Fuel', game.fuel / game.stats.maxFuel, '#22c55e'); drawBar(14, 58, 150, 11, 'Wear', Math.min(1, game.wear / game.stats.wearLimit), game.wear / game.stats.wearLimit > 0.75 ? '#ef4444' : '#f59e0b'); ctx.fillStyle = 'rgba(4,17,29,.75)'; roundRect(14, 74, 290, 28, 14); ctx.fill(); ctx.fillStyle = '#fff'; ctx.font = '900 11px Arial'; ctx.textAlign = 'left'; ctx.fillText(`Run +${game.coins}C +${game.parts}P +${game.tools}T  Hazard ${nextHazardMeters()}m`, 28, 93); if (game.eventTimer > 0) drawOutlinedText(game.eventText, w / 2, 96, '16px'); }
function drawBar(x, y, w, h, label, pct, color) { ctx.fillStyle = 'rgba(4,17,29,.72)'; roundRect(x, y, w, h, 6); ctx.fill(); ctx.fillStyle = color; roundRect(x, y, w * clamp(pct, 0, 1), h, 6); ctx.fill(); ctx.fillStyle = '#fff'; ctx.font = '800 9px Arial'; ctx.textAlign = 'left'; ctx.fillText(label, x + w + 6, y + 9); }
function currentHazard() { return game.hazards.find(z => game.x >= z.x && game.x <= z.x + z.length); }
function nextHazard() { return game.hazards.find(z => z.x + z.length > game.x); }
function nextHazardMeters() { const n = nextHazard(); return n ? Math.max(0, routeMeters(game.route, n.x - game.x)) : 0; }
function triggerEvent(text) { game.eventText = text; game.eventTimer = 1.25; }
function impactDamageFor(hazard) { if (!hazard) return { slow: 1, wear: 0, band: damageBand() }; const kmh = speedKmh(); const band = damageBand(kmh); const brakeCut = input.brake ? 0.42 : 1; const suspensionCut = clamp(1.08 - (saveData.upgrades.suspension || 1) * 0.035, 0.48, 1); const typeMult = hazard.type === 'pothole' ? 1.45 : hazard.type === 'rough' ? 0.72 : hazard.type === 'gravel' ? 0.42 : 0.22; const slow = hazard.type === 'mud' ? 0.72 : hazard.type === 'gravel' ? 0.86 : hazard.type === 'rough' ? 0.90 : 0.80; return { slow, wear: band.damage * typeMult * brakeCut * suspensionCut / game.stats.durability, band }; }
function updateTelemetry(hazard = null, band = null) { const kmh = speedKmh(); const b = band || damageBand(kmh); window.__rrTelemetry = { speedKmh: kmh, hazardSpeedKmh: kmh, damageLabel: b.label, nextHazardMeters: nextHazardMeters(), hazardType: hazard?.type || nextHazard()?.type || null }; }
function update(dt) { if (!game || game.finished) return; const h = canvas.height, route = game.route, targetPitch = routeAngle(route, game.x, h), slope = Math.sin(targetPitch), hazard = currentHazard(); let hazardSlow = 1, continuousWear = 0; if (hazard) { const effect = impactDamageFor(hazard); hazardSlow = effect.slow; if (!hazard.entered) { hazard.entered = true; game.hazardsCleared += 1; triggerEvent(`${hazard.type.toUpperCase()} AHEAD`); } if (hazard.type === 'pothole' && !hazard.impacted && game.x > hazard.x + hazard.length * 0.45) { hazard.impacted = true; game.wear += effect.wear; triggerEvent(`${effect.band.label} POTHOLE`); updateTelemetry(hazard, effect.band); } if (hazard.type !== 'pothole') continuousWear = effect.wear * 0.13; }
  const gasForce = input.gas && game.fuel > 0 ? game.stats.acceleration * game.stats.climb : 0; const gravityForce = -slope * 92; const rollingDrag = game.stats.drag + Math.abs(game.speed) * 0.018; if (gasForce > 0) { game.speed += gasForce * dt; game.fuel -= game.stats.fuelUse * dt * (1 + Math.max(0, slope) * 0.8); } game.speed += gravityForce * dt; if (input.brake) { if (game.speed > 8) game.speed -= game.stats.brakePower * dt; else if (game.x > 82) game.speed -= game.stats.reverseTop * dt; } if (game.speed > 0) game.speed -= rollingDrag * dt; if (game.speed < 0) game.speed += rollingDrag * 0.55 * dt; game.speed = clamp(game.speed, -game.stats.reverseTop, game.stats.topSpeed); game.x += game.speed * dt * hazardSlow; game.x = Math.max(80, game.x); game.distancePx = Math.max(0, game.x - 80); game.distanceM = routeMeters(route, game.distancePx); game.pitchVel += (targetPitch - game.pitch) * game.stats.spring * dt; game.pitchVel -= game.pitchVel * game.stats.damping * dt; game.pitch += game.pitchVel * dt; game.suspensionTravel = Math.sin(game.elapsed * 18) * Math.min(6, Math.abs(game.speed) / 38) + targetPitch * 6; const stress = Math.abs(targetPitch - game.pitch); game.wear += (stress * Math.abs(game.speed) * 0.012 + continuousWear) * dt * 60 / game.stats.durability; collectPickupsAndCheckpoints(); if (game.elapsed - game.lastSample > 0.18) { game.samples.push({ t: Number(game.elapsed.toFixed(2)), x: Math.round(game.x) }); game.lastSample = game.elapsed; } if (game.eventTimer > 0) game.eventTimer -= dt; completeMission(false); updateTelemetry(hazard); if (game.fuel <= 0 || game.wear >= game.stats.wearLimit || game.distancePx >= game.route.length) finishRun(game.distancePx >= game.route.length); updateHud(); }
function collectPickupsAndCheckpoints() { for (const p of game.pickups) { if (p.collected || Math.abs(game.x - p.x) > 42) continue; p.collected = true; if (p.type === 'fuel') { game.fuel = Math.min(game.stats.maxFuel + 24, game.fuel + 16); triggerEvent('Fuel +'); } if (p.type === 'parts') { game.parts += 1; saveData.parts += 1; triggerEvent('Part +1'); } if (p.type === 'tools') { game.tools += 1; saveData.tools = (saveData.tools || 0) + 1; game.wear = Math.max(0, game.wear - game.stats.repairPower); game.usedRepair = true; saveData.lifetimeRepairs += 1; triggerEvent('Tool repair'); } if (p.type === 'coin') { const coins = Math.round(4 * game.route.reward); game.coins += coins; saveData.coins += coins; } saveGameData(); }
  for (const c of game.checkpoints) { if (c.used || Math.abs(game.x - c.x) > 68) continue; c.used = true; if (c.type === 'fuelStation') { game.fuel = Math.min(game.stats.maxFuel + 36, game.fuel + game.stats.maxFuel * 0.42); triggerEvent('Fuel Station'); } if (c.type === 'partsStore') { game.parts += 3; saveData.parts += 3; triggerEvent('Auto Parts +3'); } if (c.type === 'mechanicShop') { game.wear = Math.max(0, game.wear - game.stats.wearLimit * 0.45); game.usedRepair = true; saveData.lifetimeRepairs += 1; triggerEvent('Mechanic Repair'); } if (c.type === 'wreck') { game.parts += 2; saveData.parts += 2; game.wear = Math.max(0, game.wear - 8); game.usedRepair = true; triggerEvent('Salvage +2'); } saveGameData(); } }
function missionComplete() { if (!game || !game.mission) return false; const m = game.mission, wearPct = Math.floor(Math.min(100, game.wear / game.stats.wearLimit * 100)), fuelPct = Math.floor(Math.max(0, game.fuel / game.stats.maxFuel * 100)); if (m.targetParts && game.parts >= m.targetParts) return true; if (m.needRepair && game.usedRepair) return true; if (m.targetHazards && game.hazardsCleared >= m.targetHazards && (!m.maxWearPct || wearPct <= m.maxWearPct)) return true; if (m.target && game.distanceM >= m.target && (!m.maxWearPct || wearPct <= m.maxWearPct) && (!m.minFuelPct || fuelPct >= m.minFuelPct)) return true; return false; }
function completeMission(show = true) { const m = game.mission; if (!m || saveData.completedMissions.includes(m.key) || !missionComplete()) return; saveData.completedMissions.push(m.key); saveData.coins += m.rewardCoins; saveData.parts += m.rewardParts; if (show) triggerEvent(`Mission +${m.rewardCoins}C`); saveGameData(); }
function finishRun(completed) { if (game.finished) return; game.finished = true; const bonus = Math.floor(game.distanceM / 75); saveData.coins += bonus; saveData.parts += game.parts; if (game.distanceM > saveData.bestDistance) { saveData.bestDistance = Math.floor(game.distanceM); saveData.bestTrail = game.samples.slice(-520); } completeMission(false); saveGameData(); const title = completed ? 'Route Complete' : game.wear >= game.stats.wearLimit ? 'Vehicle Worn Out' : 'Out of Fuel'; const copy = `${Math.floor(game.distanceM)}m reached. Bonus +${bonus} coins. Run collected +${game.coins} coins, +${game.parts} parts, +${game.tools} tools.`; showEndPanel(title, copy); updateHud(); }
function showEndPanel(title, copy) { const p = document.querySelector('[data-rr-end-panel]'); if (!p) return; p.querySelector('[data-rr-end-title]').textContent = title; p.querySelector('[data-rr-end-copy]').textContent = copy; p.hidden = false; }
function hideEndPanel() { const p = document.querySelector('[data-rr-end-panel]'); if (p) p.hidden = true; }
function loop(t) { if (!document.querySelector('#screen-race.active .roadRunnerShell')) { frameId = requestAnimationFrame(loop); return; } const dt = Math.min((t - lastTime) / 1000 || 0.016, 0.05); lastTime = t; if (game) { game.elapsed += dt; update(dt); draw(); } frameId = requestAnimationFrame(loop); }
function resizeCanvas() { if (!canvas) return; const host = document.querySelector('#roadRunnerGameHost'); if (!host) return; const rect = host.getBoundingClientRect(), w = Math.max(320, Math.floor(rect.width || 420)), h = Math.max(320, Math.floor(rect.height || 340)); canvas.width = w; canvas.height = h; canvas.style.width = `${w}px`; canvas.style.height = `${h}px`; }
function bindControls() { document.querySelectorAll('[data-rr-control]').forEach(btn => { const c = btn.getAttribute('data-rr-control'); const set = (v, e) => { e?.preventDefault(); if (c === 'gas') input.gas = v; if (c === 'brake') input.brake = v; btn.classList.toggle('active', v); }; btn.addEventListener('pointerdown', e => { btn.setPointerCapture?.(e.pointerId); set(true, e); }, { passive: false }); btn.addEventListener('pointerup', e => set(false, e), { passive: false }); btn.addEventListener('pointercancel', e => set(false, e), { passive: false }); btn.addEventListener('pointerleave', e => set(false, e), { passive: false }); }); window.onkeydown = e => { if (!document.querySelector('#screen-race.active')) return; if (e.key === 'ArrowRight' || e.key.toLowerCase() === 'd') input.gas = true; if (e.key === 'ArrowLeft' || e.key.toLowerCase() === 'a') input.brake = true; }; window.onkeyup = e => { if (e.key === 'ArrowRight' || e.key.toLowerCase() === 'd') input.gas = false; if (e.key === 'ArrowLeft' || e.key.toLowerCase() === 'a') input.brake = false; }; }
function mountRoadRunner(force = false) { const screen = document.querySelector('#screen-race.active'); if (!screen) return; if (force || screen !== mountedScreen || !screen.querySelector('.roadRunnerShell')) { saveData = loadSave(); screen.innerHTML = shellHtml(); mountedScreen = screen; canvas = screen.querySelector('#roadRunnerCanvas'); ctx = canvas.getContext('2d'); bindControls(); resizeCanvas(); resetRun(); renderMissionPanel(); renderVehiclePanel(); renderGaragePanel(); draw(); } if (!frameId) { lastTime = performance.now(); frameId = requestAnimationFrame(loop); } }
window.restartHillRoute = () => { input.gas = false; input.brake = false; resetRun(); draw(); };
window.setHillGhosts = m => { if (!GHOST_MODES[m]) return; activeGhostMode = m; mountRoadRunner(true); };
window.setHillRoute = r => { if (!ROUTES[r] || !routeUnlocked(r)) return; activeRoute = r; mountRoadRunner(true); };
window.selectRoadRunnerVehicle = k => { if (!saveData.unlockedVehicles.includes(k)) return; saveData.selectedVehicle = k; saveGameData(); resetRun(); renderVehiclePanel(); updateHud(); draw(); };
window.unlockRoadRunnerVehicle = k => { const item = VEHICLES[k]; if (!item || saveData.unlockedVehicles.includes(k)) return; if (saveData.coins < item.unlock.coins || saveData.parts < item.unlock.parts) return; saveData.coins -= item.unlock.coins; saveData.parts -= item.unlock.parts; saveData.unlockedVehicles.push(k); saveData.selectedVehicle = k; saveGameData(); resetRun(); renderVehiclePanel(); updateHud(); draw(); };
window.buyRoadRunnerUpgrade = k => { const l = saveData.upgrades[k] || 1, item = UPGRADES[k]; if (!item || l >= item.max) return; const c = upgradeCost(k, l); if (saveData.coins < c.coins || saveData.parts < c.parts) return; saveData.coins -= c.coins; saveData.parts -= c.parts; saveData.upgrades[k] = l + 1; saveGameData(); renderGaragePanel(); resetRun(); updateHud(); draw(); };
function inject() { const screen = document.querySelector('#screen-race.active'); if (!screen) return; requestAnimationFrame(() => mountRoadRunner(false)); }
window.addEventListener('resize', () => { resizeCanvas(); draw(); });
const observer = new MutationObserver(() => inject());
observer.observe(document.documentElement, { childList: true, subtree: true, attributes: true, attributeFilter: ['class'] });
window.addEventListener('load', inject);
document.addEventListener('click', () => requestAnimationFrame(inject));
