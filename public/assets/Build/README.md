# Build Mode Override Assets

Replace these files with final GUI art for the Build page. Keep the same names and paths so the code can load the new art automatically.

The Build page publishes a shared state snapshot for World and Lines through `365_build_communication_v1` in local storage and `window.__buildCommunicationState`.

